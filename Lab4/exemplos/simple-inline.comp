/* Programa para demonstrar a funcionalidade  do analisador sintático implementado */  programa DemontraLab {{{  var {  int (turma, idade)  carac (nome[20])  carac (inicial) }  funcao int CalculaMedia ()   var {  int (P1, P2, final) } comandos {  ler (P1);  ler (P2);  final = (P1 + P2) / 2;  retornar final; }   principal  var {  logic (aprovado)  carac (c) }  comandos {  idade = 25; turma = 21; inicial = 'G';  aprovado = falso;   escrever ("Calcular a media? (s/n): ");  ler (c);  se (c == 's' || c == 'S') {  se (CalculaMedia() >= 6.5)  aprovado = verdade;  } }  }}}