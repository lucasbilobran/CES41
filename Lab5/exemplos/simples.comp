programa ExemploSimples {{{

   var {
      int (med, compara)
      carac (toso)
   }

    principal

    var {
        int (i, inf, sup, teste[4, 5, 6])
        logic (achou)
        carac (c)
    }
    comandos {
        i = 5;
        toso = 's';
        escrever ("\nDigite a palavra: ");
        ler (c);

        enquanto ( i < 30 ) {
            i = i + 1;
        };

        se (compara == 1)
            med = 3;
        se (compara == 0)
            achou = verdade;
        senao se (compara < 0)
            sup = med - 1;
        senao 
            inf = med + 1;
        
        i = 5;
        toso = 'a';

        repetir {
            inf = inf*inf;
            i = i + 1;
        } enquanto ( i < 30 );

        teste[i+1, 2, 3-i+6] = inf - 21 * sup;

    } 
}}}