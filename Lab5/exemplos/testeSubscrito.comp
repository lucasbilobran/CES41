programa teste {{{

    principal
    var {
        int (i, j, k, l, m, n, A[10,10], B[10, 20, 30, 40, 50]) 
    }
    comandos {
        i = 7; j = 5;
        k = A[i-3,j+2] + 5;
        A[10-i,9-j] = i + j * k;
        ler (i, j, A[2,3], k);
        ler (A[1,2]);
        ler (A[3,7], i, j, k, l, B[i+j, j-k, 10, k-2, 3*i - 2*j - k], m, n);
    }
}}}
