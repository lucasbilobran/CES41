programa ExemploSimples {{{

    var {
        int (i)
    }

    principal

    var {
        logic (toso)
    }
    comandos {
        i = 1;

        enquanto (i < 5) {
            i = i + 1;
        }

        repetir {
            i = i + 3;
            se (i == 300)
                toso = verdade;
        } enquanto (toso == falso);

    } 
}}}