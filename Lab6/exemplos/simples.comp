programa ExemploSimples {{{

   var {
      int (glob)
   }

   funcao int Inserir (int a, int b)

   var {
       int (c)
   }
   comandos {
        c = a + b;
        retornar (c + glob); 
   }

    principal

    var {
        int (i, res, f)
    }
    comandos {
        i = 3;
        f = 2;
        res = Inserir(i, f);

    } 
}}}