programa ExemploSimples {{{

   var {
      int (i, j, m, A[10, 20, 30])
   }

    principal

    var {
        
    	logic (b1, b2, b3)
		real (k)
    }
    comandos {
      i = ~4;
      j = ~i*3;
      k = i / j;
      m = i % j;
      b2 = verdade;
      b3 = !b2;
      b1 = (i+3*j >= j/2-2)  &&  !b2 || (b1 != b2) && (i == j);
      A[3, 4, 5] = 21;
      i = A[3, 4, 5];
    } 
}}}